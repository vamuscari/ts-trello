import {jest, describe, expect, test} from '@jest/globals';
import { TsTrello } from "../generated";

const tsTrello = new TsTrello()

describe('Request', () => {
    test('trello', async () => {
       tsTrello.d


        expect(7).toEqual(200);
    });
})